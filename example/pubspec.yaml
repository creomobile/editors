name: example
description: Example App for 'editors' package

version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  combos: ^1.1.3
  editors: any
  demo_items:
    git: https://github.com/creomobile/demo_items

dependency_overrides:
  editors:
    path: ../

dev_dependencies:
  pedantic: ^1.8.0

flutter:
  uses-material-design: true
